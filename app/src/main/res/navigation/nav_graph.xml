<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/nav_graph"
    app:startDestination="@id/homeFragment">

    <!-- Home Fragment -->
    <fragment
        android:id="@+id/homeFragment"
        android:name="com.example.fitme.HomeFragment"
        android:label="Home"
        tools:layout="@layout/fragment_home" />

    <!-- Progress/Analytics Fragment -->
    <fragment
        android:id="@+id/progressFragment"
        android:name="com.example.fitme.ProgressFragment"
        android:label="Progress"
        tools:layout="@layout/fragment_progress" />

    <!-- Profile Fragment -->
    <fragment
        android:id="@+id/profileFragment"
        android:name="com.example.fitme.ProfileFragment"
        android:label="Profile"
        tools:layout="@layout/fragment_profile" />

    <!-- Camera Fragment -->
    <fragment
        android:id="@+id/cameraFragment"
        android:name="com.example.fitme.CameraFragment"
        android:label="Camera"
        tools:layout="@layout/fragment_camera" />

    <!-- Search Fragment -->
    <fragment
        android:id="@+id/searchFragment"
        android:name="com.example.fitme.SearchFragment"
        android:label="Search"
        tools:layout="@layout/fragment_search" />

</navigation>
